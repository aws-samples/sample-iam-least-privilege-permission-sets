# Generated by LP2PS — do not edit by hand.
#
# ⚠️ ACCOUNT ASSIGNMENT 은 의도적으로 주석 처리되어 있습니다.
# LP2PS 는 Permission Set '정의'까지만 산출하며, 실제 멤버계정 권한 부여(누가 어느 계정에서 이 PS 를
# 쓸지)는 **사람이 검토 후 수동으로** 결정합니다(불변식 ①: account assignment 는 도구가 하지 않음).
# 아래는 참고용 골격입니다. 대상 principal(그룹/사용자)·계정을 확인한 뒤 주석을 해제하세요.
{% for ps in permission_sets %}
# --- {{ ps.persona }} ({{ ps.member_count }} principals observed) ---
# resource "aws_ssoadmin_account_assignment" "{{ ps.resource_name }}_example" {
#   instance_arn       = var.identity_center_instance_arn
#   permission_set_arn = aws_ssoadmin_permission_set.{{ ps.resource_name }}.arn
#   principal_id       = "<IDENTITY_STORE_GROUP_OR_USER_ID>"  # 사람이 지정
#   principal_type     = "GROUP"                               # or USER
#   target_id          = "<AWS_ACCOUNT_ID>"                    # 사람이 지정
#   target_type        = "AWS_ACCOUNT"
# }
{% endfor %}
