## 개요
이 PR이 무엇을·왜 바꾸는지.

## 변경 유형
- [ ] 버그 수정
- [ ] 기능 추가
- [ ] 문서
- [ ] 인프라(CDK)
- [ ] 리팩터링

## 체크리스트
- [ ] **불변식 준수**: 분석 대상(멤버) 계정에 쓰기 API를 추가하지 않음(읽기 전용). 완화 예외(tooling IdC PS 정의 생성)를 건드렸다면 승인 게이트·account assignment 미수행을 유지.
- [ ] 결정론 코어(engine)를 바꿨다면 동일 입력 → 동일 산출물 유지.
- [ ] 테스트 추가/갱신: `cd engine && pytest`, `cd backend && pytest`, `cd infra && npm test` 통과.
- [ ] 프론트 변경 시 `npm run build`(타입체크 포함) 통과, 실제 렌더 확인.
- [ ] 시크릿·계정 ID·내부 식별자를 커밋에 포함하지 않음.
- [ ] 라이선스: copyleft(GPL/LGPL/AGPL/SSPL) 의존성 추가 없음.

## 테스트
어떻게 검증했는지(명령·결과).
